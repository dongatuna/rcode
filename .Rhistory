N = 10
index <- seq(1, N)
P <- (index - 0.5)/N
P
theoretical.quantity<-qnorm(P)
theoretical.quantity<-qnorm(P)
theoretical.quantity<-qnorm(P)
theoretical.quantity<-qnorm(P)
theoretical.quantity
yields<-c(86.2, 85.7, 71.9, 95.3, 77.1, 71.4, 68.9, 78.9, 86.9, 78.4)
mean.yield <- mean(yields)
sd.yield <- sd(yields)
yields.z <- (yields - mean.yield)/sd.yield
yields.z.sorted <- sort(yields.z)
theoretical.quantity<-qnorm(yields.z.sorted)
theoretical.quantity
theoretical.quantity<-qnorm(yields.z.sorted)
theoretical.quantity
theoretical.quantity<-qnorm(yields.z.sorted)
theoretical.quantity
P
theoretical.quantity<-qnorm(P)
theoretical.quantity
yields.z <- sort(yields - mean.yield)/sd.yield
yield.quantity<-qnorm(yields.z)
yield.quantity
plot(theoretical.quantity, yields.z, type = "p")
install.packages("car")
library("car", lib.loc="~/R/win-library/3.4")
library(car)
qqPlot(yields.z)
library(car)
qqPlot(yields)
